<%= simple_form_for @pin|| Pin.new, remote: true, multipart: true do |f| %>

  <%= f.input :title %>
  <%= f.input :link %>
  <%= f.input :photo %>
  <%= f.input :notes %>
  <%= f.submit class: "btn btn-success btn-block" %>

<% end %>
